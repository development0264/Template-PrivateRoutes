
const initialState = {
    userName: ""
};
const commonReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return initialState;
    }
}
export { commonReducer }